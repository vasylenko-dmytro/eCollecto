# Use a static JWK set URI in tests so Spring does not try to connect to Keycloak at startup.
# Any @SpringBootTest that exercises JWT paths must supply a mock JWT decoder bean.
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8180/realms/ecollecto/protocol/openid-connect/certs

